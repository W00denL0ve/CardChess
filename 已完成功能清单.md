# 🎮 项目已完成功能清单

> **项目类型**：卡牌 + 战棋策略游戏（回合制）  
> **技术栈**：Unity (C#) + Addressables + Tilemap + ScriptableObject  
> **架构风格**：数据与表现分离、事件驱动、状态模式、双选择器效果系统

---

## 一、项目结构目录

```
Assets/
├── Scripts/
│   ├── Core/                              # 核心基础设施
│   │   ├── Boot/                          #   游戏启动引导
│   │   │   ├── Bootstrapper.cs            │     启动引导器，顺序初始化全局管理器
│   │   │   └── Initializer.cs             │     初始化器
│   │   ├── Event/                         #   事件系统
│   │   │   ├── GameEventChannel.cs        │     全局事件总线（发布/订阅）
│   │   │   ├── GameEvents.cs              │     游戏生命周期事件
│   │   │   ├── BootEvent.cs               │     启动相关事件
│   │   │   ├── GridEvent.cs               │     网格更新事件
│   │   │   ├── TurnEvents.cs              │     回合阶段切换事件
│   │   │   ├── UnitEvent.cs               │     单位战斗事件（受伤/死亡/移动）
│   │   │   ├── InputEvent.cs              │     输入事件（单击/双击/右键/ESC）
│   │   │   └── UIEvent.cs                 │     场景/面板切换事件
│   │   └── Input/                         #   输入系统
│   │       └── InputManager.cs            │      New Input System 输入管理器
│   │
│   ├── Game/                              # 游戏业务逻辑
│   │   ├── Buff/                          #   Buff 系统
│   │   │   ├── Buff.cs                    │     Buff 抽象基类（以 BuffInstance 为参数）
│   │   │   ├── BuffInstance.cs            │     Buff 实例（持有 Unit 引用、修饰器追踪）
│   │   │   ├── BuffContainer.cs           │     Buff 容器（堆叠/驱散/过期清理）
│   │   │   └── Buffs/                     │     具体 Buff 实现
│   │   │       ├── PoisonBuff.cs          │       中毒（每回合扣血）
│   │   │       └── DefenseStanceBuff.cs   │       防御姿态（减伤）
│   │   │
│   │   ├── Card/                          #   卡牌系统
│   │   │   ├── Card.cs                    │     卡牌 MonoBehaviour（UI交互）
│   │   │   ├── CardData.cs                │     卡牌数据资产（ScriptableObject）
│   │   │   └── DeckManager.cs             │     牌库管理（抽牌/出牌/弃牌）
│   │   │
│   │   ├── Attribute/                     #   属性系统（从 Character 提取）
│   │   │   ├── Attribute.cs               │     属性值（支持修饰器叠加运算）
│   │   │   ├── AttributeManager.cs        │     属性管理器（非 MonoBehaviour，平级类）
│   │   │   └── Modifier.cs                │     修饰器（加算/乘算/最终加/最终乘）
│   │   │
│   │   ├── Effect/                        #   效果系统（双选择器架构）
│   │   │   ├── Core/                      │     核心接口与基类
│   │   │   │   ├── ITarget.cs             │       可作用目标接口
│   │   │   │   ├── TargetCombination.cs   │       目标组合模式枚举
│   │   │   │   └── Effect.cs              │       效果基类（双选择器）
│   │   │   ├── EffectContext.cs           │     效果上下文（anchor1/anchor2）
│   │   │   ├── EffectManager.cs           │     效果管理器
│   │   │   ├── TargetSelector.cs          │     目标选择器基类
│   │   │   ├── Selectors/                 │     选择器实现
│   │   │   │   ├── Anchor1Selector.cs     │       返回锚点1
│   │   │   │   ├── Anchor2Selector.cs     │       返回锚点2
│   │   │   │   ├── CasterAsUnitSelector.cs│       施法者自身
│   │   │   │   ├── AllEnemiesSelector.cs  │       所有敌人
│   │   │   │   ├── AllAlliesSelector.cs   │       所有友方
│   │   │   │   ├── CellsAroundAnchor1Selector.cs│ 锚点1周围格子
│   │   │   │   └── CasterCellsAroundSelector.cs  │ 施法者周围格子
│   │   │   ├── Effects/                   │     具体效果实现
│   │   │   │   ├── DamageEffect.cs        │       伤害效果
│   │   │   │   ├── MoveEffect.cs          │       移动效果
│   │   │   │   ├── AddBuffEffect.cs       │       添加Buff效果
│   │   │   │   └── SwapEffect.cs          │       交换位置效果
│   │   │   └── Targets/                   │     目标类型包装
│   │   │       ├── UnitTarget.cs          │       单位目标
│   │   │       ├── CellTarget.cs          │       格子目标
│   │   │       └── CardTarget.cs          │       卡牌目标
│   │   │
│   │   ├── Grid/                          #   网格系统（数据与表现分离）
│   │   │   ├── Data/                      │     网格数据层
│   │   │   │   ├── CellData.cs            │       格子数据结构
│   │   │   │   ├── LevelGridData.cs       │       关卡网格数据资产
│   │   │   │   └── TerrainType.cs         │       地形类型枚举
│   │   │   └── Runtime/                   │     网格运行时
│   │   │       ├── Cell.cs                │       运行时格子对象（OccupyingUnit）
│   │   │       ├── GridManager.cs         │       网格管理器（PlaceUnit/BFS寻路/事件订阅）
│   │   │       ├── GridVisualizer.cs      │       网格可视化（高亮/还原/双渲染器支持）
│   │   │       ├── PathRenderer.cs        │       路径绘制组件（对象池）
│   │   │       └── TerrainConfig.cs       │       地形配置资产
│   │   │
│   │   ├── Level/                         #   关卡系统
│   │   │   ├── LevelData.cs               │     关卡主数据资产
│   │   │   └── LevelManager.cs            │     关卡管理器
│   │   │
│   │   ├── Map/                           #   大地图系统
│   │   │   └── MapGenerator.cs            │     随机地图生成器
│   │   │
│   │   ├── Preview/                       #   预览系统
│   │   │   ├── PreviewManager.cs          │     预览管理器（目前实现网格预览，状态机/三角尖/遮罩/路径，后续其余预览也在其中增加）
│   │   │   └── OverlayManager.cs（已删除） │     叠加层管理器（旧版）
│   │   │
│   │   ├── States/                        #   状态模式基础设施
│   │   │   ├── IState.cs                  │     状态接口
│   │   │   └── ITurnState.cs              │     回合阶段状态接口
│   │   │
│   │   ├── Turn/                          #   回合系统
│   │   │   ├── TurnManager.cs             │     回合管理器（状态机）
│   │   │   ├── LevelTurnData.cs           │     回合行动数据资产
│   │   │   ├── TurnAction.cs              │     回合行动类体系
│   │   │   └── TurnActionExecutor.cs      │     回合行动执行器
│   │   │
│   │   ├── UI/                            #   UI 系统
│   │   │   ├── UIManager.cs               │     全局UI管理器
│   │   │   ├── HandUI.cs                  │     手牌UI
│   │   │   ├── HUD_UI.cs                  │     游戏内HUD
│   │   │   ├── MainMenuUI.cs              │     主菜单UI
│   │   │   ├── PauseMenuUI.cs             │     暂停菜单UI
│   │   │   ├── MapUITemp.cs               │     地图UI
│   │   │   ├── LoadingScreen.cs           │     加载画面
│   │   │   ├── PlayArea.cs                │     出牌区域
│   │   │   ├── AnimatedPanel.cs           │     面板动画组件
│   │   │   ├── ButtonTween.cs             │     按钮补间动画
│   │   │   ├── MaskAlphaController.cs     │     遮罩Alpha控制
│   │   │   └── MaskRadiusAnimator.cs      │     镂空遮罩动画
│   │   │
│   │   ├── Unit/                          #   单位系统
│   │   │   ├── Unit.cs                    │     战斗实体核心（MonoBehaviour）
│   │   │   ├── Occupation.cs              │     职业枚举（Warrior/Rogue/Mage）
│   │   │   ├── DamageType.cs              │     伤害类型枚举（Physical/Magical）
│   │   │   ├── AttributeInitData.cs       │     属性初始化数据
│   │   │   └── UnitConfig.cs              │     配置资产 ScriptableObject
│   │   │
│   │   ├── AudioManager.cs                │     音频管理器
│   │   ├── GameManager.cs                 │     游戏主管理器
│   │   ├── Player.cs                      │     玩家对象
│   │   └── SaveManager.cs                 │     存档/设置管理器
│   │
│   ├── Scene/                             # 场景工具
│   │   ├── SceneManager.cs                │     场景加载管理器
│   │   └── Floor.cs                       │     [占位]
│   │
│   ├── Tile/                              # 旧版Tile系统（已弃用）
│   │   └── Tile.cs                        │     旧版格子对象
│   │
│   ├── Test/                              # 测试
│   │   └── TestTextListener.cs            │     测试文本监听器
│   │
│   └── Utils/Math/                        # 工具
│       └── CurveHelper.cs                 │     贝塞尔曲线辅助
│
├── Settings/                              # 项目设置
│   └── Input/
│       ├── GameInput.cs                   │     New Input System 强类型包装类
│       └── GameInput.inputactions         │     输入动作资产（Click/ContextMenu/Escape）
│
├── Editor/                                # 编辑器工具
│   ├── LevelDataMenuExtractor.cs          │     关卡数据提取器
│   ├── TerrainTile.cs                     │     地形 Tile 资产
│   ├── EnemySpawnTile.cs                  │     敌人生成 Tile 资产
│   ├── CellChangeTile.cs                  │     格子变化 Tile 资产
│   └── SolidColorSpriteGeneratorWindow.cs │     纯色精灵生成器

└── ScriptableObjects/                     # 运行时资产目录
    └── LevelData/                         #     关卡数据资产
        ├── GridData/                      │       网格数据子资产
        └── TurnData/                      │       回合数据子资产
```

---

## 二、模块核心职责

### 🔧 Core — 核心基础设施

| 子模块 | 已完成功能 |
|--------|-----------|
| **启动引导** | • `Bootstrapper` 从 Resources 加载全局 Manager 预制体并 `DontDestroyOnLoad`<br>• 订阅场景/面板切换事件，完成后自动销毁引导器 |
| **事件总线** | • 泛型事件总线 `GameEventChannel`，按事件类型注册/分发<br>• 支持所有 `GameEvent` 派生类的发布订阅<br>• 已定义事件：`GameStartEvent`、`GamePauseEvent`、`GameOverEvent`、`PhaseChangedEvent`、`CellUpdatedEvent`、`SceneChangedEvent`、`PanelSwitchedEvent`、`UnitHealthChangedEvent`、`UnitDeathEvent`、`UnitMoveRequestEvent`、`UnitMovedEvent`、`CellLeftClickedEvent`、`CellDoubleClickedEvent`、`CellRightClickedEvent`、`EscapePressedEvent` 等 |
| **输入系统** | • 基于 **New Input System**（Input System Package）<br>• `GameInput` 强类型包装类，内嵌 InputActionAsset<br>• 单击/双击区分：`MultiTapInteraction` + `tapCount` 判定<br>• 左键/右键/ESC 全部通过 `GameEventChannel.Dispatch` 分发<br>• 兼容旧版 `WaitForCellSelection` 回调机制 |

---

### 🃏 Card — 卡牌系统

| 已完成功能 |
|-----------|
| • `CardData` ScriptableObject 资产：卡名、描述、插图、效果列表 |
| • `Card` MonoBehaviour：鼠标悬停预览、点击出牌、UI 更新 |
| • `DeckManager` 单例：抽牌（随机）、出牌（移入手牌→弃牌堆）、弃牌、手牌判定 |

---

### � Attribute — 属性系统

| 已完成功能 |
|-----------|
| • `AttributeManager` 重构为**普通 C# 类**（非 `MonoBehaviour`），由 `Unit` 构造持有 |
| • `Attribute` 属性值系统：基础值 + 修饰器叠加 → `FinalValue`<br>• 四种修饰器类型：`Add`（加算）、`Multiply`（乘算）、`FinalAdd`（最终加）、`FinalMultiply`（最终乘） |
| • 完整 API：`AddAttribute` / `SetBaseValue` / `GetBaseValue` / `GetFinalValue` / `AddModifier` / `RemoveModifier` / `RemoveModifiersFromSource` / `HasAttribute` |
| • 预定义属性类型：`Health`、`MaxHealth`、`Attack`、`Intelligence`、`DamageBonus`、`PhysicalDefense`、`MagicDefense`、`ActionPointLimit`、`ActionPoints` |
| • `Character.cs` 已删除，职能由 `Unit` + `AttributeManager` + `BuffContainer` 替代 |

---

### 🩹 Buff — Buff 系统（重构）

| 已完成功能 |
|-----------|
| • `Buff` 抽象基类重构：所有回调改为以 `BuffInstance` 为参数，新增 `OnUnitMove` / `OnAfterDamageTaken` / `OnBeforeDamageTaken(ref int)`；<br>内置 `AddModifier` / `RemoveModifier` 辅助方法 |
| • `BuffInstance` 重构：持有 `Unit Host` 引用、`CurrentStacks` 堆叠计数、`SourceContext`；<br>`RegisterModifier` / `UnregisterModifier` 追踪修饰器，`Cleanup()` 自动清理 |
| • **`BuffContainer` 新增**：管理单位所有活跃 Buff：`ApplyBuff`（堆叠/刷新）、`RemoveBuff`、`GetAllBuffs`、`OnTurnStart` / `OnTurnEnd`（自动过期清理） |
| • `PoisonBuff`：通过 `instance.Host.TakeDamage()` 造成伤害 |
| • `DefenseStanceBuff`：通过 `AddModifier` 添加 `PhysicalDefense` 乘算修饰器 |

---

### ⚡ Effect — 效果系统（双选择器架构）

| 已完成功能 |
|-----------|
| **核心** |
| • `ITarget` 接口：`GetWorldPosition()` / `GetCellPosition()` / `gameObject` |
| • `TargetCombination` 四种配对模式：`CrossProduct` / `Zip` / `FirstOfA_AllB` / `AllA_FirstOfB` |
| • `Effect` 基类：持有 `selectorA` + `selectorB` + `combination`，`Apply()` 入口自动配对后调用抽象 `ApplyToPair()` |
| • `EffectContext` 结构体：`caster` / `anchor1` / `anchor2` / `sourceCard` / `customParams` |
| **选择器**（7 个） |
| • `Anchor1Selector` / `Anchor2Selector` — 直接返回对应锚点 |
| • `CasterAsUnitSelector` — 施法者自身 |
| • `AllEnemiesSelector` / `AllAlliesSelector` — 阵营全体（需 LevelManager 配合） |
| • `CellsAroundAnchor1Selector` / `CasterCellsAroundSelector` — 半径范围格子 |
| **效果**（4 个） |
| • `DamageEffect` — 对目标单位造成伤害（支持 `DamageType` 防御计算） |
| • `MoveEffect` — 将源单位移动到目标格子（通过 `Unit.RequestMove` 事件驱动） |
| • `AddBuffEffect` — 通过 `BuffContainer.ApplyBuff` 添加 Buff |
| • `SwapEffect` — 交换两个单位位置（通过 `Unit.RequestMove` 事件驱动） |
| **目标包装**（3 个） |
| • `UnitTarget` — 包装 `Unit`，移除 `Character` 依赖 |
| • `CellTarget` / `CardTarget` |

---

### 📐 Grid — 网格系统（数据与表现分离）

| 已完成功能 |
|-----------|
| **数据层** |
| • `TerrainType` 枚举：`ground` / `water` / `unreachable` |
| • `CellData` 结构体：地形类型 + 高度 |
| • `LevelGridData` 资产：宽高 + 一维 CellData 数组，提供 `GetCell(col,row)` |
| **运行时** |
| • `Cell` 对象：col/row/terrainType/height/isWalkable/`OccupyingUnit`(Unit)/activeEffects |
| • `GridManager` 单例：加载网格数据、`WorldToGrid` / `GridToWorld` / `GetWorldPosition` 坐标转换、<br>  `PlaceUnit` 放置单位、`FindPath` BFS 寻路、`GetReachableCells` BFS 可达区域、<br>  订阅 `UnitMoveRequestEvent` / `UnitDeathEvent` 自动管理格子占用 |
| • `GridVisualizer`：生成/更新网格视觉，支持双渲染器（`MeshRenderer` / `SpriteRenderer`），<br>  `HighlightCells` 高亮（跳过被占格子，保存原始材质）、`ClearHighlights` 还原 |
| • **`PathRenderer` 新增**：路径绘制组件，线段+箭头对象池（20个），`ShowPath` / `HidePath` |
| • `TerrainConfig` 资产：配置各地形类型的可行走性和视觉材质 |

---

### 🗺️ Level — 关卡系统

| 已完成功能 |
|-----------|
| • `LevelData` 主资产：持有 `gridData` + `turnData` 子资产引用 |
| • `LevelManager`：关卡初始化时调用 `GridManager.LoadGridData()` + `TurnManager.LoadTurnData()`<br>• `allUnits` 列表管理所有单位：`RegisterUnit` / `UnregisterUnit` / `AllUnits`<br>• `GetEnemiesOf(unit)` / `GetAlliesOf(unit)` / `GetUnitsOf(faction)` — 基于 `Faction` 和 `IsAlive` 查询<br>• 订阅 `UnitDeathEvent` 自动 `UnregisterUnit` |

---

### 🔄 Turn — 回合系统

| 已完成功能 |
|-----------|
| • **状态机架构**：`TurnPhase` 枚举（Start/Draw/PlayerPlay/PlayerAction/Enemy/End）+ `ITurnState` 接口 + 6 个具体状态类 |
| • `TurnManager` 单例：回合计数、阶段切换、加载回合数据、`CurrentRoundActions` 属性 |
| • **回合行动数据**：`LevelTurnData` 资产（`[SerializeReference]` 多态序列化）<br>• 支持多回合、每回合多个 `TurnAction` |
| • **回合行动类**：`TurnAction` 抽象基类 → `EnemySpawnAction` / `CellChangeAction` / `EffectApplyAction` |
| • **行动执行器**：`TurnActionExecutor` 运行时按类型分派执行<br>• 敌人生成、格子属性修改（通过 `GridManager.SetCell` 触发可视化更新）、效果应用 |
| • `StartState.Enter()` 自动加载并执行当前回合的预设行动 |

---

### ⚔️ Unit — 单位系统（替换 Character）

| 已完成功能 |
|-----------|
| **枚举与数据结构** |
| • `Occupation`：`Warrior` / `Rogue` / `Mage` |
| • `DamageType`：`Physical` / `Magical` |
| • `AttributeInitData` 结构体：类型 + 基础值 |
| **配置资产** |
| • `UnitConfig` ScriptableObject：unitId / unitName / occupation / icon / initialAttributes / innateBuffs |
| **核心 Unit 类**（MonoBehaviour） |
| • `Initialize(config, faction, gridPos)` — 从配置初始化属性系统 + Buff 容器 |
| • 便捷属性：`CurrentHealth` / `MaxHealth` / `Attack` / `Intelligence` / `DamageBonus` / `PhysicalDefense` / `MagicDefense` / `ActionPointLimit` / `ActionPoints` |
| • `TakeDamage(finalDamage, context)` — 计算最终伤害，触发 `UnitHealthChangedEvent` / `UnitDeathEvent` |
| • `Heal(amount, context)` — 治疗，触发 `UnitHealthChangedEvent` |
| • `RequestMove(targetPos, context)` — 发布 `UnitMoveRequestEvent`，事件驱动 `GridManager` 处理 |
| • `GetDefenseFor(DamageType)` — 按伤害类型返回对应防御值 |
| • **不实现** `ITarget`，通过 `UnitTarget` 包装交互 |

---

### 🗺️ Map — 大地图

| 已完成功能 |
|-----------|
| • `MapGenerator` 单例：地图生成入口（具体生成逻辑待实现） |

---

### 👁️ Preview — 网格预览系统（重写）

| 已完成功能 |
|-----------|
| • `PreviewManager` 单例 + **状态机**（`Idle` / `Selecting` / `Preselected`） |
| • **三角尖管理**：`markerPrefab` 实例化 + `Animator` 触发 Show/Hide 动画 |
| • **遮罩管理**：控制相机子物体 `PreviewMask` 的激活状态 |
| • **预览生命周期**：`EnterGridPreview(unit, cells, confirm, cancel)` → 高亮 → 选择 → `ConfirmSelection` / `CancelPreview` |
| • **输入处理**：通过 `GameEventChannel` 订阅 `CellLeftClickedEvent` / `CellDoubleClickedEvent` / `CellRightClickedEvent` / `EscapePressedEvent` |
| • **路径绘制**：`GridManager.FindPath` + `PathRenderer.ShowPath` |
| • **双击确认**：从 `InputManager` 的 MultiTap 交互直接分发 `CellDoubleClickedEvent` |
| • **退出清除**：自动清除高亮、路径、三角尖、遮罩 |

---

### 🖥️ UI — 用户界面

| 已完成功能 |
|-----------|
| • `UIManager` 全局管理：面板注册/实例化/显示隐藏/层级栈管理、背景遮罩、转场镂空遮罩 |
| • `HandUI`：手牌布局、添加/移除卡牌 |
| • `MainMenuUI` / `PauseMenuUI` / `HUD_UI` / `LoadingScreen` / `MapUITemp` / `PlayArea` |
| • 动画组件：`AnimatedPanel`（面板动画）、`ButtonTween`（按钮补间）、`MaskAlphaController` / `MaskRadiusAnimator`（遮罩动画） |

---

### 🔊 其他系统

| 模块 | 已完成功能 |
|------|-----------|
| **音频** | `AudioManager` 单例 |
| **存档/设置** | `SaveManager`：`SettingsData` 结构体（音量/分辨率/帧率/画质）+ `DemoPath` 工具类 |
| **场景管理** | `SceneManager` 单例：异步场景加载 |

---

### 🛠️ Editor — 编辑器工具

| 已完成功能 |
|-----------|
| • **`TerrainTile`**：地形 Tile 资产（terrainType + height），在 Tilemap 上绘制基础地形 |
| • **`EnemySpawnTile`**：敌人生成 Tile 资产（enemyId + count） |
| • **`CellChangeTile`**：格子变化 Tile 资产（targetTerrain + heightDelta + setWalkable） |
| • **`LevelDataMenuExtractor`**：`Tools/Extract LevelData From Scene`<br>  - 从 `Base` Tilemap 提取地形网格 → `LevelGridData`<br>  - 从 `RoundN` Tilemap 解析回合行动 → `LevelTurnData`<br>  - 坐标自动转换为逻辑坐标（相对基础网格原点）<br>  - 自动注册 Addressables 组 |
| • **`SolidColorSpriteGeneratorWindow`**：纯色精灵图生成器 |

---

## 三、架构亮点

```
事件驱动: 所有事件（含输入事件） → GameEventChannel → 各 Manager 解耦
状态模式: TurnManager (状态机) → Start/Draw/PlayerPlay/PlayerAction/Enemy/End
          PreviewManager (状态机) → Idle/Selecting/Preselected
双选择器: Effect (selectorA + selectorB + combination) → 灵活的目标-效果配对
数据分离: Grid Data (ScriptableObject) ↔ Grid Runtime (Manager + Visualizer)
Unit 抽象: 统一战斗实体，AttributeManager + BuffContainer 双组件组合
修饰器系统: 基础值 → Add → Multiply → FinalAdd → FinalMultiply 四层运算
事件驱动移动: Unit.RequestMove → UnitMoveRequestEvent → GridManager → CellUpdatedEvent + UnitMovedEvent
New Input System: GameInput 包装类 + MultiTap 单击/双击区分 → GameEventChannel 分发
可扩展: TurnAction / Effect / TargetSelector / Buff 均为抽象基类，新增只需派生
对象池: PathRenderer 池化管理路径精灵，避免频繁 Instantiate/Destroy
编辑器管线: Tilemap 绘制 → 菜单工具一键提取 → ScriptableObject 资产 → 运行时加载
```

---

*更新日期：2026-05-16*

---

*生成日期：2026-05-14*
