Unity 中 **3D 物体（MeshRenderer）与 2D 精灵（SpriteRenderer）的渲染排序统一问题**。好消息是它们完全可以协同工作，而且你已经在编辑器中配置好了 Render Queue，只需要让 2D 精灵也进入这套体系即可。

---

## 两种排序系统的关系

| 系统              | 控制方式                               | 底层实现                      |
| --------------- | ---------------------------------- | ------------------------- |
| MeshRenderer 材质 | `Material.renderQueue`             | 直接设定 Render Queue 值       |
| SpriteRenderer  | `Sorting Layer` + `Order in Layer` | 内部转化为 Render Queue 的一个子范围 |

**核心原则**：所有透明物体的最终绘制顺序由 **Render Queue** 决定（值越小越先渲染），同 Queue 内再按距离排序。因此，只要让 2D 精灵也使用自定义材质并设定 Render Queue，就能和 3D 地格精确对齐。

---

## 推荐方案：为 2D 精灵创建材质，统一用 Render Queue 管理

### 1. 创建材质文件（编辑器操作）

在 Project 窗口中分别创建以下材质（右键 → Create → Material）：

- `OverlayMask.mat`：Shader 选择 `Sprites/Default`（或 URP 中的 `Sprite-Lit` 等）

- `PathLine.mat`：同上

- `MarkerTriangle.mat`：同上

### 2. 设置 Render Queue

选中每个材质文件，在 Inspector 中：

- 勾选 **"Advanced Options"**（如果使用内置 Shader）或在 URP 中展开 **"Surface Options"**。

- 找到 **"Render Queue"** 下拉菜单，选择 **"Transparent"**，然后手动输入数值：
  
  - `OverlayMask.mat` → `3000`
  
  - `PathLine.mat` → `3200`
  
  - `MarkerTriangle.mat` → `3300`（或保留三角尖的 Sorting Order 机制，为简单直接用 Queue）

**重要**：确保材质的 `ZWrite` 设为 `Off`，`ZTest` 为 `LEqual`（默认），避免透明物体错误地阻挡其他透明物体。如果不设置，可能会因深度测试导致后面的透明物体被裁掉，但通常影响不大。

### 3. 将材质赋给 SpriteRenderer

在创建遮罩、路径精灵、三角尖时，通过代码赋值材质：

csharp

// 遮罩
SpriteRenderer overlaySR = overlayMask.AddComponent<SpriteRenderer>();
overlaySR.sharedMaterial = overlayMaskMaterial; // 直接引用 .mat 文件
// 路径精灵
SpriteRenderer lineSR = lineObj.GetComponent<SpriteRenderer>();
lineSR.sharedMaterial = pathLineMaterial;
// 三角尖（已在预制体上设置材质，或代码设置）
preselectMarker.GetComponent<SpriteRenderer>().sharedMaterial = markerMaterial;

使用 `sharedMaterial` 避免运行时创建实例，直接使用资产材质（编辑器已设好 Queue）。

### 4. 为什么这样更好？

- **统一排序**：所有物体（地格 Mesh、遮罩、路径、标记）都在同一套 Render Queue 数值下排序，不会出现 Sprite Order 和 Material Queue 打架的情况。

- **精确控制**：你可以精细调整每个元素之间的绘制顺序（例如 `3100` vs `3110`），而 Sorting Order 的步长很大且不透明。

- **零额外代码**：材质配置都在编辑器完成，运行时只做替换，无 GC 开销。

---

## 备选方案（如果你想继续用 Sorting Order）

如果出于某些原因不想创建额外材质（但总共也就 3 个），也可以保持 SpriteRenderer 的 Sorting Order，但需要知道默认 Sorting Layer 对应的 Queue 范围。默认的 `Default` Sorting Layer 底层 Render Queue 大约从 `3000` 开始，`Order in Layer` 每增加 1，Queue 增加一个很小的偏移（可能 10 或 100？实际是乘以 `SortingLayer.GetLayerValueFromID` 的权重，默认权重为 0，Order 的影响很小）。这意味着你很难用 Order 去精确对齐 `3000`、`3100`、`3200` 这样的值，很可能顺序错乱。

**因此强烈推荐方案一（统一材质 Queue）**，只需花 5 分钟创建三个材质，从此排序再无困扰。

---

## 最终渲染顺序确认

| 物体          | 类型      | Render Queue                       |
| ----------- | ------- | ---------------------------------- |
| 默认地格        | 3D Mesh | Geometry (2000) 或自定义 Transparent 值 |
| 半透明遮罩       | Sprite  | 3000                               |
| 可选高亮格子      | 3D Mesh | 3100                               |
| 路径精灵（短线/箭头） | Sprite  | 3200                               |
| 三角尖标记       | Sprite  | 3300                               |

这样无论 2D 还是 3D，都按照 Queue 值从后往前正确叠加，遮罩在最下，三角尖在最上。
